/* istanbul ignore file */
export const CLAIM_INHERITANCE_TYPES = {
  INHERITANCE_TYPE_ALL: 1, // Claim is inherited every time
  INHERITANCE_TYPE_NONE: 2, // Claim is never inherited
  INHERITANCE_TYPE_PRODUCT: 3, // Claim is inherited if destination product # remains the same
};
